name: PR CI

on:
  pull_request:
    branches: [ develop ]
    paths:
      - 'sql/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    services:
      mariadb:
        image: mariadb:10.11
        env:
          MARIADB_ROOT_PASSWORD: pass
          MARIADB_DATABASE: dev
        ports:
          - 3306:3306
        options: --health-cmd="healthcheck.sh --connect --innodb_initialized" --health-interval=10s --health-timeout=5s --health-retries=3
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Atlas
        uses: ariga/setup-atlas@v0

      - name: 1. Check for existing SQL modification/deletion
        run: |
          # Use SHAs provided by the GitHub event for reliable diffing
          BASE_SHA=${{ github.event.pull_request.base.sha }}
          HEAD_SHA=${{ github.event.pull_request.head.sha }}
          
          echo "Comparing $BASE_SHA to $HEAD_SHA"
          
          # get list of modified files in the sql/common directory
          MODIFIED_FILES=$(git diff --name-only $BASE_SHA $HEAD_SHA -- sql/common/)
          
          for file in $MODIFIED_FILES; do
            if [[ "$file" == *.sql ]]; then
              # Check if the file already exists in the base commit
              if git show $BASE_SHA:"$file" > /dev/null 2>&1; then
                echo "Error: Modification of existing SQL file '$file' is not allowed."
                exit 1
              fi
            fi
          done

      - name: 2. Check Naming Convention
        run: |
          BASE_SHA=${{ github.event.pull_request.base.sha }}
          HEAD_SHA=${{ github.event.pull_request.head.sha }}
          
          # Check naming convention for NEW files (Added files only)
          NEW_FILES=$(git diff --name-status $BASE_SHA $HEAD_SHA -- sql/common/ | grep '^A' | awk '{print $2}')
          
          for file in $NEW_FILES; do
            if [[ "$file" == *.sql ]]; then
              filename=$(basename "$file")
              # Pattern: {Timestamp}_{Version}_{Description}.sql
              # Regex: ^[0-9]{14}_v[0-9]+\.[0-9]+\.[0-9]+_.*\.sql$
              if [[ ! $filename =~ ^[0-9]{14}_v[0-9]+\.[0-9]+\.[0-9]+_.*\.sql$ ]]; then
                echo "Error: '$filename' does not follow the naming convention: {Timestamp}_{Version}_{Description}.sql"
                echo "Example: 20250320153000_v1.9.0_my-feature.sql"
                exit 1
              fi
            fi
          done

      - name: 3. Atlas Validate (includes Integrity Check)
        run: |
          # Validate ensures that migration files match the atlas.sum hash
          atlas migrate validate --dir "file://sql/common"

      - name: 4. Atlas Lint
        run: |
          # Lint against the dev-database (MariaDB)
          atlas migrate lint \
            --dir "file://sql/common" \
            --dev-url "mariadb://root:pass@localhost:3306/dev" \
            --latest 1
